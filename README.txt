The main directory is for publishing
The development directory is for testing directives while you code.
To make hte development directory to work, you need to add symlink, app, pointing to directives .js directory

`ln -s ../../master app`
